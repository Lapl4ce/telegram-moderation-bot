# 🤖 Telegram Moderation Bot

Полнофункциональный Telegram-бот для модерации чата с системой опыта, рейтингов и множественными функциями управления сообществом.

## 🌟 Основные функции

### 🛡️ Модерация
- Система предупреждений (`/warn`, `/unwarn`)
- Заглушение пользователей с временными ограничениями (`/mute`, `/unmute`)
- Блокировка пользователей (`/ban`, `/unban`)
- Автоматическая блокировка после максимального количества предупреждений
- Сохранение всех действий модерации в базе данных

### 🎫 Тикет-система
- Создание тикетов в личных сообщениях (`/ticket`)
- Автоматическая пересылка в группу администраторов
- Ответы на тикеты через команды (`/reply`)
- Блокировка пользователей от создания тикетов
- Управление статусом тикетов

### 👤 Профиль пользователя
- Детальная статистика пользователя (`/profile`)
- Система опыта и уровней по формуле: `exp = 3 * level² + 50 * level + 100`
- Подсчет сообщений и активности
- Отображение рейтинга среди других пользователей
- Прогресс-бар до следующего уровня

### 👑 Административные команды
- Изменение опыта пользователей (`/xpmodify`)
- Изменение уровня пользователей (`/levelmodify`)
- Управление правами доступа (`/give_rights`, `/remove_rights`)
- Множители опыта (`/multiplier`)
- Кастомные роли и титулы

### 🏆 Рейтинги и топы
- Топ пользователей по опыту (`/top`)
- Топ пользователей по уровню (`/toplevel`)
- Топ пользователей по арт-очкам (`/topartpoints`)
- Пагинация для больших списков
- Отображение текущей позиции пользователя

### 🎨 Арт-очки
- Система арт-очков для творческой активности
- Управление арт-очками (`/modify_artpoints`)
- Специальные права для арт-лидеров
- Рейтинг по арт-очкам

### ⚡ Автоматическое начисление опыта
- Случайное начисление 5-20 XP за сообщение
- Кулдаун 20 секунд между начислениями
- Множители опыта для особых пользователей
- Автоматические уведомления о повышении уровня

### 🚫 Модерация слов
- Добавление запрещенных слов (`/add_badword`)
- Автоматическое удаление сообщений с запрещенными словами
- Управление списком запрещенных слов (`/list_badwords`, `/remove_badword`)
- Статистика фильтра слов

## 🏗️ Архитектура

### Структура проекта
```
telegram-moderation-bot/
├── bot.py                 # Главный файл запуска
├── config.py             # Конфигурация и настройки
├── database/
│   ├── __init__.py
│   ├── models.py         # Модели данных
│   └── database.py       # Работа с базой данных
├── handlers/
│   ├── __init__.py
│   ├── common.py         # Общие команды
│   ├── moderation.py     # Команды модерации
│   ├── tickets.py        # Тикет-система
│   ├── profile.py        # Профили пользователей
│   ├── admin.py          # Админ-команды
│   ├── top.py           # Рейтинги и топы
│   ├── roles.py         # Кастомные роли
│   ├── artpoints.py     # Арт-очки
│   └── badwords.py      # Модерация слов
├── middleware/
│   ├── __init__.py
│   └── auth.py          # Аутентификация и права
├── utils/
│   ├── __init__.py
│   ├── experience.py    # Логика опыта и уровней
│   ├── time_parser.py   # Парсинг времени
│   └── validators.py    # Валидация данных
├── requirements.txt
├── .env.example
├── .gitignore
└── README.md
```

### Система прав
- **👤 USER** - обычный пользователь
- **🛡️ MODERATOR** - модератор (команды модерации)
- **👑 ADMIN** - администратор (все команды)
- **🎨 ART_LEADER** - арт-лидер (управление арт-очками)

## 🚀 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/Lapl4ce/telegram-moderation-bot.git
cd telegram-moderation-bot
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка окружения
Скопируйте `.env.example` в `.env` и заполните необходимые данные:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```bash
# Telegram Bot Configuration
BOT_TOKEN=your_bot_token_here
ADMIN_GROUP_ID=-100123456789
TICKETS_GROUP_ID=-100987654321

# Database
DATABASE_PATH=bot_database.db

# Logging
LOG_LEVEL=INFO
LOG_FILE=bot.log
```

### 4. Получение токена бота
1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Вставьте токен в `.env` файл

### 5. Настройка групп (опционально)
- `ADMIN_GROUP_ID` - ID группы для уведомлений администраторов
- `TICKETS_GROUP_ID` - ID группы для тикетов

Для получения ID группы:
1. Добавьте бота в группу
2. Отправьте сообщение в группу
3. Проверьте логи бота для получения ID

### 6. Запуск бота
```bash
python bot.py
```

## 📋 Команды бота

### 👤 Пользовательские команды
- `/start` - приветствие и информация о боте
- `/help` - полный список команд
- `/profile` - показать профиль пользователя
- `/top` - топ пользователей по опыту
- `/toplevel` - топ пользователей по уровню
- `/topartpoints` - топ пользователей по арт-очкам
- `/artpoints [пользователь]` - показать арт-очки
- `/ticket сообщение` - создать тикет (только в ЛС)

### 🛡️ Команды модерации (для модераторов)
- `/warn [пользователь] [причина]` - выдать предупреждение
- `/mute [пользователь] [время] [причина]` - заглушить пользователя
- `/ban [пользователь] [время] [причина]` - заблокировать пользователя
- `/unwarn [пользователь]` - снять предупреждение
- `/unmute [пользователь]` - разглушить пользователя
- `/unban [пользователь]` - разблокировать пользователя
- `/reply [ID_тикета] [ответ]` - ответить на тикет

### 🎨 Арт-команды (для арт-лидеров)
- `/modify_artpoints [пользователь] [±количество]` - изменить арт-очки

### 👑 Админ-команды (для администраторов)
- `/xpmodify [пользователь] [±количество]` - изменить опыт
- `/levelmodify [пользователь] [уровень]` - изменить уровень
- `/give_rights [пользователь] [права]` - выдать права
- `/remove_rights [пользователь]` - убрать права
- `/multiplier [пользователь] [множитель]` - изменить множитель опыта
- `/assign_role [пользователь] [роль]` - назначить кастомную роль
- `/unassign_role [пользователь]` - убрать кастомную роль
- `/set_title [пользователь] [титул]` - установить титул
- `/remove_title [пользователь]` - убрать титул
- `/add_badword [слово]` - добавить запрещенное слово
- `/remove_badword [слово]` - убрать запрещенное слово
- `/list_badwords` - список запрещенных слов
- `/badwords_stats` - статистика фильтра слов

## ⚙️ Конфигурация

### Настройки опыта
- **Минимальный XP за сообщение:** 5
- **Максимальный XP за сообщение:** 20
- **Кулдаун между начислениями:** 20 секунд
- **Формула уровня:** `exp = 3 * level² + 50 * level + 100`

### Настройки модерации
- **Максимальное количество предупреждений:** 3
- **Автобан при превышении:** 24 часа

### Форматы времени
Поддерживаемые форматы для команд модерации:
- `1d` - 1 день
- `2h` - 2 часа
- `30m` - 30 минут
- `1d2h30m` - 1 день 2 часа 30 минут

## 🗄️ База данных

Бот использует SQLite базу данных со следующими таблицами:

### users
Информация о пользователях, их опыт, уровни и настройки

### punishments
История наказаний (предупреждения, заглушения, блокировки)

### tickets
Система тикетов для поддержки пользователей

### bad_words
Список запрещенных слов для автомодерации

### user_roles
Кастомные роли пользователей

## 🔧 Разработка

### Добавление новых команд
1. Создайте новый handler в папке `handlers/`
2. Зарегистрируйте router в `bot.py`
3. Добавьте необходимые права через middleware

### Расширение базы данных
1. Обновите модели в `database/models.py`
2. Добавьте новые методы в `database/database.py`
3. Создайте миграции для существующих данных

### Тестирование
Для тестирования создайте отдельного бота и используйте его токен в окружении разработки.

## 📝 Логирование

Бот ведет подробные логи всех действий:
- Команды модерации
- Изменения прав и статусов
- Ошибки и исключения
- Создание и ответы на тикеты

Логи сохраняются в файл, указанный в `LOG_FILE` (по умолчанию `bot.log`).

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 🆘 Поддержка

Если у вас возникли проблемы или вопросы:
1. Проверьте логи бота
2. Убедитесь в правильности конфигурации
3. Создайте Issue в GitHub репозитории

## 🔄 Обновления

Для обновления бота:
1. Остановите текущий процесс
2. Выполните `git pull`
3. Установите новые зависимости `pip install -r requirements.txt`
4. Запустите бота заново

---

Создано с ❤️ для эффективной модерации Telegram сообществ